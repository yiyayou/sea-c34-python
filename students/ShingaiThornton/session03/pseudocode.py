def mail_room():

    reply = none

    def make_donor_list():
        donor_list = [] 
        donor_name = [name, total_donated, number_of_donations, average_donation_amount]
        for donor_name in donor_list:
            add donor_name to donor_list

    def ask_input():
        reply = choose_thanks_or_create_report_press_q_to_quit(prompt)
        if user_input == "q":
            end program
            return reply


    def if_thank_you():
        if reply is thank you:
            donor_name = enter_donors_name(prompt)

            if donor_name = 'list':
                print donor_list
            
            if donor_name is not member of donor_list:
                add donor_name to donor_list
                selected_donor = donor_name
                return donor_name
           
            if donor_name is a member of donor_list:
                selected donor = donor_name
                return donor_name

    def donation_amount():
        
    donation_amount = enter_donation_amount(prompt)

    if type(donation_amount) is non-numeric:
        donation_amount = enter_donation_amount(prompt)
        return donation_amount

    def if_create_report():
   
    if reply is create report:
        print donor_list sorted by historical donation_amount

    


